# mariadb-c

Scaffold for the `mariadb_c` Python module. This package is intended to host C-related utilities/bindings for MariaDB.

## Development

- Install in editable mode:
  
  ```bash
  python -m pip install -e .[dev]
  ```

- Run tests:
  
  ```bash
  pytest
  ```
