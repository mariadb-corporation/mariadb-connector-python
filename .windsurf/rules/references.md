---
trigger: always_on
---

The reference protocol is MariaDB's. When MySQL and MariaDB documentation conflict, MariaDB wins. MySQL docs are a fallback only for topics not covered by MariaDB's own documentation.